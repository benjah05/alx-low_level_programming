#include "main.h"
#include<string.h>
/**
 * *_strcpy - copy the string pointed by src
 * to buffer pointed to by dest
 * @dest: buffer pointer
 * @src: originally pointed char
 * Return: the pointer to dest
 */
char *_strcpy(char *dest, char *src)
{
	return (strcpy(dest, src));
}
