#include<stdio.h>

/**
 * main - program entry point
 * Return: 0
 */
int main(void)
{
	char alphABET;

	for (alphABET = 'a'; alphabABET <= 'z'; alphABET++)
		putchar(alphABET);
	for (alphABET = 'A'; alphABET <= 'Z'; alphABET++)
		putchar(alphABET);
	return (0);
}
