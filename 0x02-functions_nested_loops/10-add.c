#include "main.h"
/**
 * add - add 2 numbers
 * @x: first number
 * @y: second number
 * Return: result(sum of 2 numbers)
 */
int add(int x, int y)
{
	int result;

	result = x + y;
	return (result);
}
